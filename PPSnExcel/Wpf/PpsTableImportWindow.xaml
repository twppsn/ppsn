<Window x:Class="PPSnExcel.Wpf.PpsTableImportWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
						 xmlns:ui="clr-namespace:TecWare.PPSn.UI;assembly=PPSn.Desktop.UI"
             xmlns:local="clr-namespace:PPSnExcel.Wpf"
             mc:Ignorable="d"
						Title="Tabelle auswählen"
				Width="400"
				Height="500"
             d:DesignHeight="300" d:DesignWidth="300" ResizeMode="CanResizeWithGrip" WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="32" />
      <RowDefinition Height="*" />
      <RowDefinition Height="50" />
    </Grid.RowDefinitions>

    <TextBox Width="200" Height="23" Margin="0,4,12,4" HorizontalAlignment="Right" Grid.Column="1" />

    <ListBox Grid.Row="1" Margin="12" Grid.ColumnSpan="2">

    </ListBox>

    <Button Grid.Row="2" Content="Einfügen" HorizontalAlignment="Left" Margin="12,13,0,13"  Width="100" Height="23" Command="{x:Static ApplicationCommands.Open}" />
    <Button Grid.Row="2" Content="Schließen" HorizontalAlignment="Right" Margin="0,13,12,13"  Width="100" Height="23" Command="{x:Static ApplicationCommands.Close}" Grid.Column="1" />
  </Grid>
</Window>
