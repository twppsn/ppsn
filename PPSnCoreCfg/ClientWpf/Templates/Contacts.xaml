<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	
	<ResourceDictionary.MergedDictionaries>
		<ResourceDictionary Source="..\Default.xaml" />
	</ResourceDictionary.MergedDictionaries>

	<DataTemplate x:Key="crmContacts,,100">
		<!--  crmContacts,dbo.contacts,100 -->
		<StackPanel Orientation="Horizontal">
            <TextBlock Text="Kontakt-Nr.: "/>
            <TextBlock Text="{Binding Nr}" />
            <TextBlock Text=" / "/>
            <TextBlock Text="{Binding Name}" />
			<TextBlock Text=" / "/>
			<TextBlock Text="{Binding LiefNr}" />
			<TextBlock Text=" / "/>
			<TextBlock Text="{Binding KundNr}" />
			<TextBlock Text=" / "/>
			<TextBlock Text="{Binding Inaktiv}" />
		</StackPanel>
	</DataTemplate>
	
</ResourceDictionary>