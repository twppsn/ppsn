<Window x:Class="TecWare.PPSn.UI.PpsCameraDialog" x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TecWare.PPSn.UI"
        mc:Ignorable="d"
        Title="Kamera" Height="450" Width="800" ResizeMode="NoResize" WindowStyle="None" Topmost="True" ShowInTaskbar="False">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="100" />
			<ColumnDefinition />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		<DockPanel LastChildFill="True">
			<Button DockPanel.Dock="Bottom" Content="Take" Command="{x:Static ApplicationCommands.New}" />
			<ListBox ItemsSource="{Binding Devices}" IsSynchronizedWithCurrentItem="True">
				<ListBox.ItemTemplate>
					<DataTemplate>
						<Grid Height="90" Width="90">
							<Grid.RowDefinitions>
								<RowDefinition />
								<RowDefinition />
							</Grid.RowDefinitions>
							<Image Grid.Row="0" Source="{Binding PreviewImage,IsAsync=True}" ></Image>
							<TextBlock Grid.Row="1" Text="{Binding Name}"></TextBlock>
						</Grid>
					</DataTemplate>
				</ListBox.ItemTemplate>
			</ListBox>
		</DockPanel>
		<Image Grid.Column="1" Source="{Binding CurrentDevice.PreviewImage,IsAsync=true}" />
		<Image Grid.Column="2" Source="{Binding CurrentImage}" />
	</Grid>
</Window>
