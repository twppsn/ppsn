<UserControl x:Class="TecWare.PPSn.UI.PpsTracePane" x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TecWare.PPSn.UI"
			 mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Background="{StaticResource PPSnWindowBackgroundBrush}">
    <UserControl.Resources>
        <CollectionViewSource x:Key="SortedTraces" Source="{Binding Traces}"
                      IsLiveFilteringRequested="True">
        </CollectionViewSource>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <DataTemplate x:Key="ExceptionTemplate">
            <StackPanel Orientation="Vertical">
                <TextBlock>
							<!--<Run Text="message:  "/>-->
							<Run FontWeight="Bold" Text="{Binding Message, Mode=OneWay}"/>
                </TextBlock>
                <TextBlock>
							<Run Text="Exception: "/>
							<Run FontWeight="Bold" Text="{Binding Exception.Message, Mode=OneWay}"/>
                </TextBlock>
                <StackPanel x:Name="source">
                    <TextBlock>
							<Run Text="source:  "/>
							<Run Text="{Binding Source, Mode=OneWay}" />
                    </TextBlock>
                </StackPanel>
                <StackPanel x:Name="targetSite">
                    <TextBlock>
							<Run Text="TargetSite:  "/>
							<Run Text="{Binding TargetSite, Mode=OneWay}" />
                    </TextBlock>
                </StackPanel>
                <StackPanel x:Name="innerException">
                    <StackPanel Orientation="Horizontal">
                        <ToggleButton x:Name="innerExceptionEnable" Content="+" Width="20"/>
                        <TextBlock Text="inner Exception:"/>
                    </StackPanel>
                    <Border BorderThickness="1" BorderBrush="White" Margin="2" Visibility="{Binding ElementName=innerExceptionEnable, Path=IsChecked, Converter={StaticResource BoolToVisibilityConverter}}">
                        <ContentControl Content="{Binding InnerException}" ContentTemplate="{DynamicResource ExceptionTemplate}"/>
                    </Border>
                </StackPanel>
                <StackPanel x:Name="stackTrace">
                    <StackPanel Orientation="Horizontal">
                        <ToggleButton x:Name="stackTraceEnable" Content="+" Width="20"/>
                        <TextBlock Text="stacktrace:"/>
                    </StackPanel>
                    <TextBlock Visibility="{Binding ElementName=stackTraceEnable, Path=IsChecked, Converter={StaticResource BoolToVisibilityConverter}}" Text="{Binding StackTrace}"/>
                </StackPanel>

            </StackPanel>

            <DataTemplate.Triggers>
                <!-- Highlight for Not Reviewed -->
                <DataTrigger Binding="{Binding Path=InnerException}" Value="{x:Null}">
                    <Setter TargetName="innerException" Property="Visibility" Value="Collapsed" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=StackTrace}" Value="{x:Null}">
                    <Setter TargetName="stackTrace" Property="Visibility" Value="Collapsed" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=TargetSite}" Value="{x:Null}">
                    <Setter TargetName="targetSite" Property="Visibility" Value="Collapsed" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=Source}" Value="{x:Null}">
                    <Setter TargetName="source" Property="Visibility" Value="Collapsed" />
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>

        <local:TraceItemTemplateSelector x:Key="TraceItemTemplateSelector">
            <local:TraceItemTemplateSelector.NullTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Message}">
                        <TextBlock.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Kopieren" Command="{x:Static local:PpsTracePane.CopyTraceCommand}" CommandParameter="{Binding Message}"/>
                            </ContextMenu>
                        </TextBlock.ContextMenu>
                    </TextBlock>
                </DataTemplate>
            </local:TraceItemTemplateSelector.NullTemplate>

            <local:TraceItemTemplateSelector.ExceptionTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">
                        <StackPanel.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Kopieren" Command="{x:Static local:PpsTracePane.CopyTraceCommand}" CommandParameter="{Binding Exception}"/>
                            </ContextMenu>
                        </StackPanel.ContextMenu>
                        <TextBlock>
							<!--<Run Text="message:  "/>-->
							<Run FontWeight="Bold" Text="{Binding Message, Mode=OneWay}"/>
                        </TextBlock>
                        <TextBlock>
							<Run Text="Exception: "/>
							<Run FontWeight="Bold" Text="{Binding Exception.Message, Mode=OneWay}"/>
                        </TextBlock>
                        <TextBlock>
                            <Run Text="Quelle:  "/>
                            <Run FontWeight="Bold" Text="{Binding Exception.Source, Mode=OneWay}" />
                        </TextBlock>
                        <TextBlock>
							<Run Text="TargetSite:  "/>
							<Run FontWeight="Bold" Text="{Binding Exception.TargetSite, Mode=OneWay}" />
                        </TextBlock>
                        <StackPanel Name="tbExceptionData">
							<Label Content="Data:  "/>
                            <DataGrid DataContext="{Binding Exception.Data.Values}"/>
                        </StackPanel>
                        <StackPanel x:Name="innerException">
                            <StackPanel Orientation="Horizontal">
                                <ToggleButton x:Name="innerExceptionEnable" Content="+" Width="20"/>
                                <TextBlock Text="inner Exception:"/>
                            </StackPanel>
                            <Border BorderThickness="1" BorderBrush="White" Margin="2" Visibility="{Binding ElementName=innerExceptionEnable, Path=IsChecked, Converter={StaticResource BoolToVisibilityConverter}}">
                                <ContentControl Content="{Binding Exception.InnerException}" ContentTemplate="{DynamicResource ExceptionTemplate}"/>
                            </Border>
                        </StackPanel>
                        <StackPanel x:Name="stackTrace">
                            <StackPanel Orientation="Horizontal">
                                <ToggleButton x:Name="stackTraceEnable" Content="+" Width="20"/>
                                <TextBlock Text="stacktrace:"/>
                            </StackPanel>
                        </StackPanel>
                        <TextBlock Visibility="{Binding ElementName=stackTraceEnable, Path=IsChecked, Converter={StaticResource BoolToVisibilityConverter}}" Text="{Binding Exception.StackTrace}"/>
                    </StackPanel>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding Path=Exception.InnerException}" Value="{x:Null}">
                            <Setter TargetName="innerException" Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=Exception.StackTrace}" Value="{x:Null}">
                            <Setter TargetName="stackTrace" Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=Exception.Data}" Value="{x:Null}">
                                    <Setter TargetName="tbExceptionData" Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </local:TraceItemTemplateSelector.ExceptionTemplate>

            <local:TraceItemTemplateSelector.TraceItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <StackPanel.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Kopieren" Command="{x:Static local:PpsTracePane.CopyTraceCommand}" CommandParameter="{Binding}"/>
                            </ContextMenu>
                        </StackPanel.ContextMenu>
                        <TextBlock Text="{Binding Message}" />
                        <TextBlock Text="{Binding Id}" />
                        <TextBlock Text="{Binding Source}" />
                    </StackPanel>
                </DataTemplate>
            </local:TraceItemTemplateSelector.TraceItemTemplate>

            <local:TraceItemTemplateSelector.TextItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Message}">
                        <TextBlock.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Kopieren" Command="{x:Static local:PpsTracePane.CopyTraceCommand}" CommandParameter="{Binding}"/>
                            </ContextMenu>
                        </TextBlock.ContextMenu>
                    </TextBlock>
                </DataTemplate>
            </local:TraceItemTemplateSelector.TextItemTemplate>

        </local:TraceItemTemplateSelector>
    </UserControl.Resources>

    <Grid>
        <ListBox ItemsSource="{Binding Source={StaticResource SortedTraces}}"	Style="{StaticResource PPSnListBoxStyle}" Foreground="{StaticResource PPSnWindowForegroundBrush}" AlternationCount="2" FontSize="13.333"
				 ItemContainerStyle="{StaticResource PPSnAlternatingListBoxItemStyle}" Background="{StaticResource PPSnWindowBackgroundBrush}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="90"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="22" />
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="{Binding Type}" />
                        <TextBlock Grid.Column="1" Text="{Binding Stamp, StringFormat=dd.MM.yyyy  HH:mm:ss}" Margin="8,0"/>
                        <ContentControl Grid.Column="2" Margin="8,0" Grid.Row="0" Grid.RowSpan="2" FocusVisualStyle="{x:Null}"
										Content="{Binding}" ContentTemplateSelector="{StaticResource TraceItemTemplateSelector}"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>

</UserControl>
