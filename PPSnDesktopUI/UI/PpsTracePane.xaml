<UserControl x:Class="TecWare.PPSn.UI.PpsTracePane" x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TecWare.PPSn.UI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <UserControl.Resources>
    <local:TraceItemTemplateSelector x:Key="TraceItemTemplateSelector">
      <local:TraceItemTemplateSelector.NullTemplate>
        <DataTemplate>
					<TextBlock Text="Null?" />
				</DataTemplate>
			</local:TraceItemTemplateSelector.NullTemplate>
      <local:TraceItemTemplateSelector.ExceptionTemplate>
        <DataTemplate>
          <TextBlock Text="Exception?" />
        </DataTemplate>
      </local:TraceItemTemplateSelector.ExceptionTemplate>
      <local:TraceItemTemplateSelector.TraceItemTemplate>
        <DataTemplate>
          <TextBlock Text="Trace?" />
        </DataTemplate>
      </local:TraceItemTemplateSelector.TraceItemTemplate>
    </local:TraceItemTemplateSelector>
	</UserControl.Resources>
  <Grid>
    <ListBox Grid.Column="0" ItemsSource="{Binding Traces}">
      <ListBox.ItemTemplate>
        <DataTemplate>
          <Grid>
            <Grid.RowDefinitions>
							<RowDefinition Height="23" />
							<RowDefinition Height="100" />
						</Grid.RowDefinitions>
						<TextBlock Text="{Binding Type}" />
            <TextBlock Text="{Binding Stamp}" />
            <TextBlock Text="{Binding Message}" />
            <ContentControl Content="{Binding}" Grid.Row="1" ContentTemplateSelector="{StaticResource TraceItemTemplateSelector}">
						</ContentControl>
          </Grid>
				</DataTemplate>
			</ListBox.ItemTemplate>
    </ListBox>
  </Grid>
</UserControl>
